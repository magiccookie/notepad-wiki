// import { fromJS } from 'immutable';
// import { makeSelectLoginFormDomain } from '../selectors';

// const selector = makeSelectLoginFormDomain();

describe('makeSelectLoginFormDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
